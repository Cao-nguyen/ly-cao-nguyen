// Example JavaScript to adjust content dynamically
window.addEventListener('resize', function() {
    const screenWidth = window.innerWidth;
    if (screenWidth <= 768) {
        // Perform actions for smaller screens, if needed
    }
});
